"""Diagram mappers.

Every module here exposes a module-level `MAPPER`. The registry imports them
all automatically, so adding a diagram type means adding exactly one file to
this package and nothing else.
"""
