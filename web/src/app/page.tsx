export default function Home() {
  return (
    <main className="mx-auto flex min-h-screen max-w-2xl flex-col justify-center gap-3 p-8">
      <h1 className="text-2xl font-semibold tracking-tight">AI Software Architect</h1>
      <p className="text-muted-foreground text-sm">
        Scaffold is up. Nothing is wired yet — the CPM, the review gate and the
        diagram pipeline land in M1.
      </p>
    </main>
  );
}
